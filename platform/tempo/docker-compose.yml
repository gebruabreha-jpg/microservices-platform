version: '3.8'

services:
  tempo:
    image: grafana/tempo:2.4.0
    ports:
      - "3200:3200"
      - "9411:9411"
    command:
      - '-config.file=/etc/tempo.yaml'
    volumes:
      - tempo_data:/tmp/tempo

volumes:
  tempo_data:
