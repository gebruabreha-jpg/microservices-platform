listeners.tcp.default = 5672
management.tcp.port = 15672
default_user = admin
default_pass = secret
default_vhost = /

# Declare exchanges, queues and bindings (incl. the notifications -> dlx -> dlq
# dead-letter topology) at boot so services never race to create them.
load_definitions = /etc/rabbitmq/definitions.json
